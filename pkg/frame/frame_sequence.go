package frame

import "image"

// @todo accept array of image.Image instead of image.Palleted

func GenerateSequence(encode func([]*image.Paletted) error, overlay overlay, screenImage []*image.Paletted) error {
	return nil
}
